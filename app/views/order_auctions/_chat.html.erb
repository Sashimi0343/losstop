
<h3>メッセージ一覧</h3>


<div class="messages">
  <% @contacts.each do |contact| %>
    <div class="message">
      <%= contact.name %>　　<span class="created_at"><%= contact.created_at %></span><br>
      <%= contact.message %>
    </div>
  <% end %>
</div>


<%= form_with model: @contact, url: auction_order_auction_contact_auctions_path(auction_id: @auction.id, order_auction_id: @order.id), local: true do |f| %>
    <%= f.text_field :message, placeholder: "メッセージを入力する" , class: "chat-form"%>
    <%= f.submit "送信" %>
<% end %>